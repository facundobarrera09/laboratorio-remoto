// When the page finishes loading, hide the loading animation
window.addEventListener('load', function () {
    var loadingContainer = document.querySelector('.loading');
    // loadingContainer.style.display = 'none';
});
